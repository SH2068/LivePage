import React from 'react';
import LoginPopup from '../../src/components/units/login/LoginPopup';  // 팝업 컴포넌트를 경로에 맞게 가져옵니다.

const LoginPageWrapper: React.FC = () => {
  return <LoginPopup />;
};

export default LoginPageWrapper;
